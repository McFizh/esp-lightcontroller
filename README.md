# ESP32 based led light controller with android client

Repo contains firmware + PCB for ESP32 based light controller. Repo also has android client written in kotlin. Client discovers the controller on LAN using mDNS.
